
@{
    ViewBag.Title = "AfterLogin";
}

<h2>After Login</h2>
@if (Session["LogedUserEmail"] != null)
{
    <text>
            
            Welcome @Session["LogedUserEmail"].ToString()

    </text>
}

<br>
@Html.ActionLink("Add new User", "CreateUser")
